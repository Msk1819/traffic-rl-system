[project]
name = "traffic-env"
version = "0.1.0"
description = "Traffic RL environment"

dependencies = [
    "fastapi",
    "uvicorn",
    "numpy",
    "openai",
    "openenv>=0.2.0"
]

[project.scripts]
server = "server.app:main"
